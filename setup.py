from setuptools import setup

setup(
   name='parsing_requests',
   version='1.0',
   description='Parsing utis for HTTP NASA',
   author='Angelos Sakellaropoulos',
   author_email='angsakel@gmail.com',
   python_requires='==2.7',
   install_requires=['mock'],
)
